/** App version, shown at the bottom of the You tab. Bump on each release so a
 *  fren can tell at a glance whether their installed PWA has the latest build
 *  (the service worker is push-only / no cache, so a reload always pulls fresh). */
export const APP_VERSION = "1.3.6";
